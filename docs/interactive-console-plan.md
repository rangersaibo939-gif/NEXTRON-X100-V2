# Interactive NEXTRON console

The next generated app milestone makes Create App and Run Agent functional locally before wiring remote AI credentials. Create App captures the requested app, Run Agent creates a deterministic multi-brain execution plan, and Build APK remains the existing CI-backed artifact path.
